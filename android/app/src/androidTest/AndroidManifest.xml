<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application>
        <service
            android:name="test.com.zeon.zeon.bg.VerificationTrafficService"
            android:exported="true"
            android:foregroundServiceType="dataSync" />
        <activity
            android:name="test.com.zeon.zeon.bg.RussianServicesValidationActivity"
            android:exported="true" />
        <activity
            android:name="test.com.zeon.zeon.bg.Http3ValidationActivity"
            android:exported="true" />
        <activity
            android:name="test.com.zeon.zeon.bg.DataPlaneValidationActivity"
            android:exported="true">
            <!-- Make the deterministic validation UID visible to the
                 product's installed-app picker in isolated acceptance builds. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
